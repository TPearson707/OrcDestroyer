#pragma once

class SceneManager {
    public:

    private:

    private:
};

// Stack of scenes
// ["Overworld"]
// user hits esc -> push Pause scene onto the stack of scenes
// ["Overworld", "Pause"]
// user hits resume
// pop the pause state off 
// ["Overworld"]